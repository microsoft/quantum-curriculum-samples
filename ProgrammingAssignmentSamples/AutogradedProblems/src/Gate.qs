import Std.Math.PI;
// Task 3. Implement a controlled Rz gate.
// Input: two qubits in an arbitrary state (stored in an array of length 2).
// Goal: Apply a controlled Rz gate, using the first qubit as control and the second qubit as target, 
//       with π/4 as the angle argument for the gate.
operation Task3(qs : Qubit[]) : Unit {
    // Write your solution here.
    // ...
}
